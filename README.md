# markdown-testing
# testing
Install deps:
```bash
npm install markdown-to-html -g
```
also have git installed then run:
```bash
bash setup.sh
```
and start the server with:
```bash
python3 server.py
```
